# =============================================================================
# RENDER BACKEND – Environment variables reference
# =============================================================================
# Render does NOT accept .env file uploads. Add each variable in the dashboard:
#   Render → Your Web Service → Environment → Add environment variable
#
# Use this file as a checklist. Replace every <...> with your real value.
# After first deploy, set APP_URL to your Render service URL and FRONTEND_URL
# to your Vercel URL, then redeploy.
# =============================================================================

# Required
APP_NAME=rocketlms
APP_ENV=production
APP_KEY=<run: php artisan key:generate --show>
APP_DEBUG=false
APP_VERSION=1.8
APP_URL=https://rocket-lms-backend.onrender.com

LOG_CHANNEL=stack
JWT_SECRET=<run: php artisan jwt:secret --show>
API_KEY=<e.g. your-secret-api-key-123>

# Frontend on Vercel (not served by Laravel). Set after Part 3.
SERVE_REACT_FROM_BACKEND=false
FRONTEND_URL=https://your-app.vercel.app

# ---------------------------------------------------------------------------
# Database – PostgreSQL on Render
# ---------------------------------------------------------------------------
# Use the INTERNAL Database URL from Render (Part 1).
# Leave DB_HOST, DB_PORT, DB_DATABASE, DB_USERNAME, DB_PASSWORD unset.
# ---------------------------------------------------------------------------
DB_CONNECTION=pgsql
DATABASE_URL=postgres://USER:PASSWORD@HOST/DATABASE?sslmode=require

# ---------------------------------------------------------------------------
# Optional – Mail (uncomment and set if you need email)
# ---------------------------------------------------------------------------
# MAIL_MAILER=smtp
# MAIL_HOST=
# MAIL_PORT=587
# MAIL_USERNAME=
# MAIL_PASSWORD=
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS=
# MAIL_FROM_NAME="Platform Title"

# ---------------------------------------------------------------------------
# Optional – S3 / MinIO, OAuth, Payments (add if you use them)
# ---------------------------------------------------------------------------
# MINIO_KEY=
# MINIO_SECRET=
# MINIO_REGION=
# MINIO_BUCKET=
# MINIO_ENDPOINT=
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# PAYPAL_CLIENT_ID=
# PAYPAL_SECRET=
# RAZORPAY_API_KEY=
# RAZORPAY_API_SECRET=

FIT_KARNATAKA_ENABLED=false
