# Backend API base URL (no trailing slash).
# Development: leave empty to use Vite proxy (backend must run on http://localhost:8000).
# Production: set to your backend URL, e.g. https://lms.rocket-soft.org
VITE_API_URL=

# Backend API key (must match backend .env API_KEY). Required for all API requests.
VITE_API_KEY=1234
