# Backend API base URL (no trailing slash).
# - When React is served FROM the backend (SERVE_REACT_FROM_BACKEND=true): leave EMPTY so API calls use same origin (/api/...).
# - Development (React on :8080, backend on :8000): leave empty; Vite proxy forwards /api to backend.
# - Production with separate frontend domain: set to backend URL, e.g. https://lms.rocket-soft.org
VITE_API_URL=

# Must match backend .env API_KEY. Sent as x-api-key on every API request.
VITE_API_KEY=1234
