import { InstructorLayout } from "@/components/InstructorLayout";

/**
 * Instructor panel wrapper: layout with sidebar; nested routes render in Outlet.
 */
const InstructorPanel = () => <InstructorLayout />;

export default InstructorPanel;
